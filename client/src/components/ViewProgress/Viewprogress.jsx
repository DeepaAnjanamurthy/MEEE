import React from 'react'

function Viewprogress(props) {
    // Get request to backend to fetch all notes for a single user
    
    return (
        <div>
            {/* map returned array to display on the page */}
        </div>
    )
}

export default Viewprogress
